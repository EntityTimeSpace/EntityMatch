var original = $('#segIntel').val();	var selectStart = 0;var selectEnd = 0;$('#segIntel').bind('keyup', function(event) {	var content = $(this).val();	if(replaceSpace(original) != replaceSpace(content)) {		$(this).val(original);		this.selectionStart = selectStart;		this.selectionEnd = selectEnd;	}	else {		original = content;		selectStart = this.selectionStart;		selectEnd = this.selectionEnd;	}});	function replaceSpace(str) {	return str.replace(/\s/g, '');}